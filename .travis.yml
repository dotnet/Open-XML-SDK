language: csharp

os:
  - linux
  - osx

mono:
  - latest
  - 4.0.5

sudo: false  # use the new container-based Travis infrastructure

before_install:
  - chmod +x build.sh

script:
  - ./build.sh All

matrix:
  allow_failures:
    - mono: latest
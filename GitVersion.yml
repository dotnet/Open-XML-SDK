mode: ContinuousDeployment
next-version: 3.0.0
branches:
  release:
    regex: ^release?[/-]
    tag: beta
    increment: None
    is-release-branch: true
  test:
    regex: ^test?
    tag: DONOTUSE
    increment: None
    is-release-branch: true
ignore:
  sha: []

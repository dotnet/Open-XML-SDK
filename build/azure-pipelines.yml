trigger:
- main
- release/*

pr:
  autoCancel: true
  branches:
    include:
    - main
    - release/*
  paths:
    exclude:
      - '**/*.md'
      - docs

variables:
  RunPeVerify: true

stages:
  - stage: Build
    jobs:
      - template: package.yml
      - template: build.yml
        parameters:
          buildTargets:
            - name: full
              vmImages: 
                - image: 'windows-latest'
                  name: 'windows'
                - image: 'ubuntu-latest'
                  name: 'linux'
            - name: net35
              TestTargetFramework: net452
              TestDependencyTargetFramework: net35
              vmImages: 
                - image: 'windows-latest'
                  name: 'windows'
            - name: netstandard20
              TestTargetFramework: net6.0
              TestDependencyTargetFramework: netstandard2.0
              vmImages: 
                - image: 'windows-latest'
                  name: 'windows'
                - image: 'ubuntu-latest'
                  name: 'linux'

  - stage: Sign
    jobs:
    - job: Sign
      displayName: Sign assemblies and package
      pool:
        vmImage: 'windows-latest'
      steps:
        - template: sign.yml
    dependsOn: Build
    condition: and(succeeded(), ne(variables['Build.Reason'], 'PullRequest'))
